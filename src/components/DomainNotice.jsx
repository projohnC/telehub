// Domain Notice popup has been disabled per request.
// The component intentionally renders nothing so no modal appears on load
// or on user interaction.
const DomainNotice = () => null;

export default DomainNotice;
